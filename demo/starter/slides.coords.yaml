# Slidev drag coords — auto-managed by the dev server.
# Tracked in git by default; add to .gitignore if you'd rather keep positions personal.
# Top-level keys are slide numbers (1-based); values are dragId → posStr.
"1":
  feat-reorder-2: 503,228,180,105,0,1000
  feat-crop: 287,172,180,105,0,100,16,16,10,7
  feat-crop-2: 288,251,180,105,0,100,28,9,28,28
  feat-drag: 85,325,180,105
  feat-rotate: 76,174,180,105,-15
  feat-reorder-1: 483,178,180,105,0,101
  feat-resize-locked: 696,174,235,137
  feat-resize: 696,327,264,93
"2":
  tweet-1390115482657726468: 54,133,280,305
  yt-luoMHjh-XcQ: 364,133,300,170
  # Initial height is a rough seed — BlueSky.vue tracks the widget's iframe
  # height via ResizeObserver and feeds it into `lock-aspect-ratio`, so the
  # wrapper height auto-snaps to width / natural-AR on load. Resize then
  # preserves the AR (corner handles), same as Tweet/YouTube. A previous
  # iteration disabled AR-locking for BSky because yaml-commit broadcasts
  # re-applying server state caused oscillation; that was fixed upstream
  # (yaml-commit no longer re-applies element state), so AR-lock is back on.
  bsky-3lgwg2vy44s2q: 695,142,260,150
"10":
  tweet-1390115482657726468: 620,110,280,305
